version: '3.8'

services:
  # Example web service
  nginx:
    image: nginx:alpine
    container_name: shackmate-web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
    networks:
      - shackmate-network

  # Example application service  
  # app:
  #   image: node:18-alpine
  #   container_name: shackmate-app
  #   working_dir: /app
  #   volumes:
  #     - ./app:/app
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=production
  #   command: npm start
  #   restart: unless-stopped
  #   networks:
  #     - shackmate-network

  # Example database service
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: shackmate-db
  #   environment:
  #     POSTGRES_DB: shackmate
  #     POSTGRES_USER: shackmate
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./db/init:/docker-entrypoint-initdb.d:ro
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped
  #   networks:
  #     - shackmate-network

  # Example monitoring service
  # portainer:
  #   image: portainer/portainer-ce:latest
  #   container_name: shackmate-portainer
  #   ports:
  #     - "9000:9000"
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - portainer_data:/data
  #   restart: unless-stopped

networks:
  shackmate-network:
    driver: bridge

volumes:
  postgres_data:
  portainer_data:
